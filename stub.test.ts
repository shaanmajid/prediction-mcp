import { test, expect } from "bun:test";

// Stub test to satisfy CI until comprehensive tests are implemented
// TODO: Remove this file once real tests are added (see TODO.md)
test("stub test passes", () => {
  expect(true).toBe(true);
});
